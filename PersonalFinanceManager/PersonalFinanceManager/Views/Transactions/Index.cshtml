@model IEnumerable<PersonalFinanceManager.Models.Transaction>

@{
    ViewBag.Title = "Samit";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Value)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Subcategory.SubcategoryName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.User.UserFirstName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Value)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Subcategory.SubcategoryName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.User.UserFirstName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TransactionsID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TransactionsID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TransactionsID })
        </td>
    </tr>
}

</table>
